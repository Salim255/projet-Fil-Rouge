name: Container
on: push

jobs:
  node-docker:
    runs-on: ubuntu-latest
    services:
      app:
        image: crawan/filrougeserversalim
        ports:
          - 9000:9000
    steps:
      - name: get to page
        run: http://localhost:9000
